name: my first workflow

on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: checkout actions
        uses: actions/checkout@v4

      - name: few linux cmds
        run: |
          echo "message from my first workflow"
          ls -lrta
          cat README.md