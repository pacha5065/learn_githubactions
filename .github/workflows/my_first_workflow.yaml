name: my first workflow

on:
  push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@v4
    
    - name: list file
      run: ls

    - name: print text
      run: echo "Hello, How are you" >> hello.txt

    - name: file exists
      run: grep -i hello