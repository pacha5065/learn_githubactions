name: learning exmple v1

on:
    workflow_dispatch:
    push:
        branches: 
        - feature/*
        - main
    pull_request: 
        branches: 
        - main
        types: 
        - opened
        - closed

jobs:
    job1:
        runs-on: ubuntu-latest
        steps:
        - name: checkout
          uses: actions/checkout@v4
          
        - run: echo "Simple workflow test"